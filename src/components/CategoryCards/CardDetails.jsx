import { useParams } from "react-router-dom";


const CardDetails = () => {
    const params = useParams()
    console.log(params);
    return (
        <div>

        </div>
    );
};

export default CardDetails;